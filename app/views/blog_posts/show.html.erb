<div class="max-w-2xl mx-auto mt-8">
  <%= link_to "Edit Blog Post", edit_blog_post_path(@blog_post), class: 'text-blue-500 hover:underline' if user_signed_in? %>

  <h1 class="text-3xl font-bold mt-4 mb-6"><%= @blog_post.title %></h1>
  <p class="text-gray-700 leading-relaxed"><%= @blog_post.body %></p>
</div>
